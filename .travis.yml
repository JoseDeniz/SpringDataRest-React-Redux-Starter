language: java
jdk:
  - oraclejdk8
deploy:
  on:
    branch: master
  branches:
    only:
      - master
   provider: heroku
   api-key:
    secure: $HEROKU_API_KEY
   app:
    master: ci-spring-boot-data-rest-react
